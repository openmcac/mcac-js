<div class="post" data-auto-id="post-view">
  {{#if post.bannerUrl}}
    <header class="post-view-banner"
            style="background-image: url('{{post.bannerUrl}}')"
            data-auto-id="post-view-banner">
    </header>
  {{/if}}
  {{#if post.title}}
    <h3 data-auto-id="title">
      {{#link-to "post.index" post}}{{post.title}}{{/link-to}}
    </h3>
  {{/if}}
  <div class="post-details">
    {{#link-to "post.index" post}}
      {{displayPublishedAt}}
    {{/link-to}}
  </div>
  <div data-test-id="content">
    {{#if snippets}}
      {{{post.snippetHtml}}}

      {{#if post.showReadMoreLink}}
        <div class="read-more">
          <div class="read-more-handle">
            {{#link-to "post.index" post}}
              <i class="icon icon-angle-right"></i>
              Read more
            {{/link-to}}
          </div>
        </div>
      {{/if}}
    {{else}}
      {{{post.contentHtml}}}
    {{/if}}
  </div>
  <div class="separator">
    <hr />
    <i class="icon icon-cma"></i>
  </div>
</div>
{{yield}}
