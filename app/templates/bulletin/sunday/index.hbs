<div class="bulletin">
  <div class="cover">
    <h1 class="bulletin-name">
      {{name}}<br/>
      <small>{{description}}</small>
    </h1>
  </div>

  <div class="bulletin-contents">
    <div class="inner">
      <div class="container">
        <div class="row">
          <section class="col-sm-6 service-order">
            {{{serviceOrderHtml}}}
          </section>
          <section class="col-sm-6 announcements">
            <ol>
              {{#each announcement in announcements}}
              <li>{{{announcement.descriptionHtml}}}</li>
              {{else}}
              <li>No announcements yet</li>
              {{/each}}
            </ol>
          </section>
        </div>
      </div>
    </div>
  </div>
</div>
