<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Date</th>
      <th>Actions</th>
    </tr>
  </thead>
  {{#each}}
    <tbody>
      <tr class="bulletin bulletin-{{id}}">
        <td class="bulletin-name">{{name}}</td>
        <td>{{publishedAt}}</td>
        <td>
          {{#link-to 'bulletin.index' this
                     class='btn btn-primary show-bulletin'}}
            Show
          {{/link-to}}

          {{#link-to 'bulletin.edit' this
                     class='btn btn-primary edit-bulletin'}}
            Edit
          {{/link-to}}

          <button class="btn btn-danger remove-bulletin"
                  {{action 'remove' this}}>Delete</button>
        </td>
      </tr>
    </tbody>
  {{/each}}
</table>
