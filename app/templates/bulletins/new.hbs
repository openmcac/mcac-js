<div class="container">
  <h2>Add a bulletin</h2>

  <form class="bulletin-form" action="#">
    <div class="row">
      <section class="service-information col-md-6">
        <h3>Service Information</h3>

        <div class="row">
          <div class="form-group col-md-10">
            <label for="bulletinName">Name</label>
            {{input id="bulletinName" value=model.name class="form-control bulletin-name"}}
          </div>
        </div>
        <div class="row">
          <div class="form-group col-md-10">
            <label for="publishedAt">Published at</label>
            {{bs-datetimepicker date=model.publishedAt class="published-at"}}
          </div>
        </div>
        <div class="row">
          <div class="form-group col-md-10">
            <label for="description">Description</label>
            {{input id="description" value=model.description class="form-control description"}}
          </div>
        </div>
        <div class="row">
          <div class="form-group col-md-10">
            <label for="banner">Banner</label>
            {{s3-upload url="/api/v1/bulletins/sign"}}
          </div>
        </div>
        <div class="row">
          <div class="form-group col-md-12">
            <label for="serviceOrder">Order</label>
            {{markdown-textarea id="serviceOrder"
                                value=model.serviceOrder
                                class="form-control service-order"
                                rows="10"}}
          </div>
        </div>

        <button type="submit" {{action 'save'}} class="btn btn-primary">
          Save
        </button>
      </section>
    </div>
  </form>
</div>
