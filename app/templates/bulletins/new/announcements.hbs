<h1>Announcements</h1>

<ul>
  {{#each announcement in model.announcements}}
    <li>
      {{announcement-editor announcement=announcement
                            add-announcement="addAnnouncement"}}
    </li>
  {{/each}}
</ul>

{{outlet}}
