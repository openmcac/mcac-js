<div class="container group-index">
  <div class="row">
    <header class="col-sm-12 group-banner"
            style="background-image: url('{{group.bannerUrl}}')"
            data-auto-id="group-banner">
    </header>
  </div>

  <div class="row group-details">
    <div class="col-xs-12 col-sm-2 col-sm-offset-1">
      <div class="profile-picture-wrapper">
        <div class="profile-picture"
           data-auto-id="group-profile-picture"
           style="background-image: url('{{group.profilePictureUrl}}')"></div>
      </div>
    </div>

    <div class="col-xs-12 col-sm-9">
      <h1 class="group-name" data-auto-id="group-name">
        {{#link-to "group.index" group.slug}}{{group.name}}{{/link-to}}
      </h1>

      <div class="short-description" data-auto-id="group-short-description">
        {{group.shortDescription}}
      </div>

      <div class="group-info">
        <span data-auto-id="group-target-audience">{{group.targetAudience}}</span>
        &middot;
        <span data-auto-id="group-meet-details">{{group.meetDetails}}</span>
      </div>
    </div>
  </div>

  {{#if posts.length}}
    <div class="row latest-news">
      <section class="col-sm-offset-2 col-sm-8 col-md-offset-3 col-md-6"
        data-auto-id="group-posts">
        <h2>Latest news</h2>
        {{#each posts as |post|}}
          {{post-view post=post}}
        {{/each}}
      </section>
    </div>
  {{/if}}

  <div class="row about-group">
    <section class="col-sm-offset-2 col-sm-8 col-md-offset-3 col-md-6">
      <h2>About {{group.name}}</h2>
      {{{group.aboutHtml}}}
    </section>
  </div>
</div>
